import { Styles } from '../../../';
import { Colors, ColorVariants } from '../../../types/styles';
import { Props as DayzedProps } from 'dayzed';
interface CalendarsProps {
    styles?: Styles;
    value?: DateObj;
    monthNames: string[];
    weekdayNames: string[];
    title?: string;
    className?: string;
    state?: CalendarState;
    selectedColor?: {
        color: Colors;
        variant: ColorVariants;
    };
    onChange?: (e: DateObj | undefined) => void;
}
interface CalendarState {
    dateObj?: DateObj;
    offset?: number;
}
interface DateObj {
    date?: Date;
    nextMonth?: boolean;
    prevMonth?: boolean;
    selectable?: boolean;
    selected?: boolean;
    today?: boolean;
}
declare const Calendars: {
    (props: CalendarsProps & DayzedProps): JSX.Element;
    defaultProps: {
        monthNames: string[];
        weekdayNames: string[];
        firstDayOfWeek: number;
        onDateSelected: () => void;
        selectedColor: string;
    };
};
export default Calendars;
