import React from 'react';
import { Styles } from '../../../';
interface CollapseProps {
    styles?: CollapseStyles;
    className?: string;
    text: string;
    isExpanded?: boolean;
}
interface CollapseStyles extends Styles {
    button?: Styles;
}
declare const Collapse: React.FC<CollapseProps>;
export default Collapse;
