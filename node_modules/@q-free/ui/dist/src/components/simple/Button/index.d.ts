import React from 'react';
import { Styles } from '../../../';
interface ButtonProps {
    styles?: Styles;
    ripple?: boolean;
}
declare const Button: React.FC<ButtonProps & React.HTMLProps<HTMLButtonElement>>;
export default Button;
